- hosts: compute-controller
  remote_user: root
  roles:
  - ubuntu-cloud
  - ntp
  - compute-controller

- hosts: compute-hypervisor
  remote_user: root
  roles:
  - ubuntu-cloud
  - ntp
  - compute-hypervisor
